const apiServer = 'http://maxUpdateThis.example.com/decks/'
export default async (qrCode) => {
  return await {
    name: 'TODO implement findDeckFromQrCode.js file',
    uuid: '321-123-321-123',
    qr_code: qrCode,
  }
  // const response = await fetch(apiServer + deckRegisterId)
  // const deck = await response.json()
  //
  // return deck
}
